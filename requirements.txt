# Core web framework
fastapi>=0.95.0
uvicorn[standard]>=0.22.0

# HTTP client
requests>=2.32.5

# WebSocket support
websockets>=12.0

# Audio processing
soundfile>=0.12.0

# PyTorch and related (for TTS model)
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# ChatterboxTTS core dependencies
transformers>=4.46.3
numpy>=1.24.0,<1.26.0
librosa==0.11.0
s3tokenizer
diffusers==0.29.0
resemble-perth==1.0.1
conformer==0.3.2
safetensors==0.5.3
huggingface-hub>=0.23.2

# Python standard library (included, listed for documentation)
# - json (builtin)
# - asyncio (builtin)
# - logging (builtin)
# - pathlib (builtin)
# - uuid (builtin)
# - tempfile (builtin)
# - subprocess (builtin)
# - os (builtin)
# - platform (builtin)
# - shutil (builtin)
# - argparse (builtin)
# - struct (builtin)
# - wave (builtin)
